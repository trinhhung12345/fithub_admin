import 'package:flutter/material.dart';

class AppColors {
  // Màu chủ đạo theo yêu cầu
  static const Color primary = Color(0xFFf64400); // Cam (Main)
  static const Color secondary = Color(0xFF00a162); // Xanh lá
  static const Color info = Color(0xFF0082ff); // Xanh dương

  // Các màu bổ trợ
  static const Color background = Color(0xFFF8F9FA);
  static const Color cardBackground = Colors.white;
  static const Color textMain = Color(0xFF1D1E20);
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color border = Color(0xFFE9ECEF);
  static const Color error = Color(0xFFDC3545);
}
